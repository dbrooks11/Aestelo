import { type JSX, useState, useEffect } from "react";
import { Globe, User, Plus, MapPin  } from "lucide-react";
import { motion, AnimatePresence } from "framer-motion";

type GlassDockProps = {
    activePage?: 'feed' | 'profile' | 
}


export default function GlassDockNav(): JSX.Element{

    return(
        <nav>

        </nav>
    )
}